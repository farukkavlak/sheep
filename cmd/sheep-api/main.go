// Command sheep-api serves the sheep control plane API.
//
// The entry point stays thin: it builds the application from the composition
// root and runs it. Nothing is wired yet.
package main

func main() {}
